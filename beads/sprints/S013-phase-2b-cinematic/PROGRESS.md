# S013 — Phase 2b Cinematic Scaffold

All tickets T061–T065 done 2026-07-12. `apps/web` typecheck passes.

## 2026-07-12 — Cinematic UX polish

- **CinematicEntryBar** on sandbox drill-in: "Tour top neighborhoods" + DC "Story mode" (replaces hidden Resume)
- Photo hero always on during flyover highlight + tour complete (not gated by 3D flag)
- MapView TextLayer: billboard + bottom/top baseline centering during cinematic tour; single-ZIP focus
- Google3DTilesBadge: subtle scaffold/ready indicators when `NEXT_PUBLIC_ENABLE_3D_TILES` is set
